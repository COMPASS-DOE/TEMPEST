Standards and blanks will be numbered starting with "0" (ie. blank_0, Blank_1, ...)

Blank_0 is 0 conc CH4 and CO2 Sample indicated as such in Sample_Type column. duplicate bland_0 for both STD

"0" is entered when a concentration was too low to detect

G_W = indicates sample is direct gas (G) or gas taken from a 1:1 water sample (W)


For AS:
check metadata for G/W status for GCW TR samples = DONE for those samples run with the TMP samples

For SW:
Ask SW what to do with GCW TR samples. For now they have been entered in with the montly runs in this folder
Is it ok to have the same STD sample ID's on same spreadsheet if the run dates are different = ID's don't really matter for the code
Are the running id's ok for the Event samples? since the monthly run id's are standard for each well = yes
Add volume column? some samples aren't 20 mL. I've made note of the few that aren't 20mL in the field notes column = no; still 1:1
Do the STD's need a month and year? = just year
Preferred date format?
Changed blank_0 to STD_CH4 - do I need to change the ID from blank_0 to CH4 or just the Sample_Type?
Should the files be in CSV or excel?


For MW:
Are the TRISTD's CHKSTD's the same? will Sample_Type be CH4 or CO2?
STD Conc for CHKSTD's 5ppm CH4? and then (per SW) should it be Sample_Type STD_CH4?